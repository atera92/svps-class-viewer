# 引き継ぎ書 — SVPS クラス別ビューア／マリガン早見表（2026-09-15 時点）

> **新しいPC（Windows）のClaudeへ：** 本人（財津大和さん）はこの続きを期待しています。経緯を説明し直させないこと。
> このフォルダの `README.md` に仕組みの詳細がある。
>
> **振る舞いのルール（本人の指示）**
> - 日本語・敬体。結論から書く。前置きと「〜しましょうか？」の確認はしない。
> - 厳しく評価する。都合の悪い事実を先に出す。楽観的な見積もりはしない。
> - 許可を取らずに実行する（パスワード入力・送金・システム設定の変更は除く）。
> - **本人の手間を最小にすることが最優先。** 手順書を渡さず、自分で済ませる。本人にしかできない操作は1回・コピペ1行まで削る。
>
> **このPCで最初にやること（全部あなたがやる）**
> 1. Python 3.10以上・Git・Google Chrome が無ければ winget で入れる（管理者の確認画面は本人が「はい」を押すだけ）
> 2. このリポジトリ（https://github.com/atera92/svps-class-viewer）を `%USERPROFILE%\yamato\tools\shadowberse` に clone し、`pip install -r requirements.txt` と `python -m playwright install chromium`
> 3. `python mull_cards.py` でカード画像とデッキ情報を公式サイトから取り直す（`cache/` はGitHubに無い。Macから運ばなくてよい）
> 4. §2 の作業を続ける。公開（git push）の初回だけ、本人にGitHubのログインをしてもらう

## 1. できているもの（公開中）

公開先：https://atera92.github.io/svps-class-viewer/ （GitHub: atera92/svps-class-viewer、GitHub Pages）

| ページ | 中身 | 状態 |
|---|---|---|
| `index.html` | プレミアシリーズ26-27の全バトルをクラス別に切り出して連続再生 | 第7節後半まで120バトル |
| `mulligan.html`（SNS・攻略記事の早見表） | Xの投稿・Dexel記事から集めたデッキ別キープ表 | 実力者＝BEYOND到達（過去・現在）の基準。11デッキに表 |
| `mulligan.html?view=stream`（プロの配信） | プロの個人配信（ランクマッチ）から読み取ったマリガン | **魔手ウィッチ：プロ5人・配信6本・80試合（全試合目視確認済み）** |

## 2. 作業中だったこと（2026-09-15、本人の最新の依頼）

> 「今日までの魔手の配信で同じことをして。全ての試合で3ターン目までの動きを書く部分を別で作り、3ターン目までの行動もパーセンテージで割合を出して」

### 2-1. 追加する魔手ウィッチの配信（9/11以降。まだ1本も読んでいない）

| 選手 | 動画ID | 公開日 | 長さ | 注意 |
|---|---|---|---|---|
| 拓海 | c8jDJLa4PUY | 9/11 | 120分 | 前半はロイヤル（バルバロス）→ 途中から魔手 |
| ミル | qA2wX3zNvjY | 9/12 | 164分 | |
| もっちゃま | vBRYyBcwstQ | 9/14 | 204分 | 題名は「ウィッチ」だけ。魔手かはカードで確認する |
| Spicies | bWnY_ir7qBY | 9/11 | 261分 | |
| rikka | eLRvSBGvvZ0 | 9/12 | 570分 | 長い。1日1本 |
| ヘイム | GAAVeq-XTis | 9/5 | 650分 | 前回、途中でYouTubeのロボット対策に止められて未読。魔手は途中の時間帯だけ |

読み方：`python mull_stream.py <動画ID> --player <名前> --class ウィッチ --deck 魔手ウィッチ --jobs 2`
→ `cache/stream_review/<動画ID>/` の確認用画像を**全試合目で確認**してから公開する（手順は §4）。

### 2-2. 3ターン目までの動き（作りかけ）

- `mull_turns.py` を作成済み。マリガン後を等倍で再生し、画面中央の「YOUR TURN」「ENEMY TURN」を見つけて、
  自分の1〜3ターン目の**始まりと終わり（次のENEMY TURN）の手札・盤面**を撮り、1試合1枚の確認用画像にする。
  手札は画面下にカード名つきで並ぶので、「始まりにあって終わりに無いカード」＝そのターンに使ったカード。
- 見本画像 `mull_ref/pv_your_turn.png`（YOUR の文字だけ。9/15に作り直し済み）・`pv_enemy_turn.png`。
  オフラインの確認では、はっきり出た瞬間を一致度1.0/0.69で検出、他の画面は0.3以下。**YOUR TURN が強く映るのは0.6秒ほどなので等倍で見る**。
- **まだ実際の試合で通して試していない**（試験中に移行の指示）。次にやること：
  1. まっつ（WrtGQMpmaRI）の1・2試合目で `find_turns` を試し、確認用画像で手札が読めるか確かめる
  2. 読み取った「使ったカード」を `stream/<ID>.json` の各試合に `"turns": [{"k":1,"played":[card_id,...]}, ...]` で入れる
     （画像を目で読んで記入する。自動では当てていない）
  3. `mulligan.html` の配信表示に、ターンごとの「使ったカード」の割合（先攻・後攻別、例：1ターン目 大遊戯世界 40%・何もしない 25%）を足す
- 3ターン目まで＝**配信者自身の1〜3ターン目（PP1〜3のターン）**と解釈している。本人に確認していないので、最初の報告で明示すること。

## 3. 必ず守ること（今回の失敗から）

1. **YouTubeのロボット対策を回避しない。** 9/11に約27時間分を続けて読んだところ「ログインして bot ではないことを確認してください」の画面が出た。
   確認画面が出ると `cache/YOUTUBE_BLOCKED` が作られて全処理が止まる（`mull_stream.py`・`mull_turns.py`）。
   代わりにログインする・別経路を使う等はしない。**1日に読むのは数本まで**。9/15には解けていた（1回だけ再生して確認）。
2. **全試合を目視確認してから公開する。** 自動の読み取りの誤りは配信の画面で大きく変わる：
   - 全面に映る配信（まっつ・rikka・ぱらちゃん）：誤り0〜2件
   - 縮小して映る配信（TBT。0.93倍に補正）：9試合で12件（先攻後攻6、カード名5、返したカード1）
   - よくある取り違え：**ストームブラスト↔正常の侵食／キティトリック、知恵の輝き↔正常の侵食**（どれもコスト1スペル）
   - 先攻・後攻は右下の表示がアバターやロゴで隠れる配信（もっちゃま）で誤る
   - リプレイ画面（下に再生ボタンと「×1.0」）はマリガン画面に見えるので除外する（ぱらちゃんで3件）
   - 観戦画面（両者の手札が映る）は本人視点の仕組みでは読めない（Era53のチーム練習。選手不明で対象外）
3. **勝敗は対戦開始画面のCRの増減で判定。CRはクラスごと**なので、デッキを持ち替える直前の試合は判定できない。
   ぱらちゃんはCRがスポンサー表示で隠れて勝敗不明。
4. 読み取りは重い。Mac（メモリ8GB）では他のアプリで空きが無くなり、ブラウザが固まった。
   固まったら5分で止めてやり直す仕組み（WATCHDOG）を入れてある。Windowsでメモリに余裕があれば `--jobs 3` でよい。

## 4. 目視確認のやり方

1. `cache/stream_review/<ID>/NN.jpg` を1枚ずつ見る（上：対戦開始画面と相手、下左：確定直前の2段、下右：交換後）
2. 画像のカード名・右下の先攻後攻と、下に書かれた読み取り結果を突き合わせる
3. 誤りは `stream/<ID>.json` の該当試合を直し、`"manual": true` と `"flags"` に何を直したかを書く
4. 相手のクラス（`"vs"`）、CR（`"cr"`）、勝敗（`"result"`）を入れる。`python mull_stream.py <ID> --player ... --class ウィッチ --vs-only` で対戦開始画面の一覧（`vs_sheet.jpg`）を作れる
5. `"checked"` に確認した旨と日付を書き、PDF作成.bat → git で公開

## 5. 未完了のもの（公開していない）

- **プレミアシリーズ（リーグ配信）からのマリガン読み取り**：`mull_scan.py`。120バトル中12バトルを古い判定で読んだところで中断。
  結果 `mull_pro.json` は**GitHubに入れていない**（移行データのzipに入っている）。返したカードの判定を直した版で読み直しが必要。
  `mull_pro.json` の `"ready": true` にするまでページには出ない。

## 6. Windowsで違うところ

- ダブルクリック用：`起動.bat`（サーバ起動）・`更新.bat`・`公開.bat`・`PDF作成.bat`・`セットアップ.bat`（最初に1回）
- Macの `.command` は使わない。コマンドは `python3` ではなく `python`
- PDFは Windows の Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`）で作る（PDF作成.bat）
- 公開（git push）には、本人が一度 `gh auth login` か Git の認証をする必要がある（パスワード入力はClaudeがしない）
